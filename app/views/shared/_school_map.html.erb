<div class="title-dashboard">
  <div class="icon-map">
    <%= image_tag "icon_map.svg", height: 30 %>
  </div>
    <h4>Carte Ecole</h4>
</div>
<div class="subtitle-dash">
  <h6> <%= link_to "https://www.google.fr/maps/dir/#{@attached_marker[:lat]},#{@attached_marker[:lng]}/#{@assign_marker[:lat]}, #{@assign_marker[:lng]}", target: '_blank', class: "default-link" do %>
    <%= image_tag("distance.svg", height: 30) %> <%= @assignment.school.address %>
   <% end %>
   </h6>
</div>
<hr>

<div id="map"
  style="width: 100%; height: 420px;"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>

